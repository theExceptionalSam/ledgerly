# Server
PORT=4000
NODE_ENV=development

# Database — PostgreSQL connection string.
# Local dev:  postgresql://user:password@localhost:5432/ledgerly
# Supabase:   postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# Render:     postgresql://user:password@dpg-xxx/render-db
DATABASE_URL=postgresql://localhost:5432/ledgerly

# Set to 'false' only for local dev without SSL. Managed hosts (Supabase, Render,
# Neon) require SSL and this should be left unset (defaults to SSL on).
# DB_SSL=false

# Frontend origins allowed by CORS (comma-separated)
CORS_ORIGINS=http://localhost:5173

# Generate strong secrets e.g. with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Do NOT use the placeholder values below — the server will refuse to start.
JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret

# Set to 'true' ONLY in local dev to have OTP codes returned in the API response.
# Never set this in production or preview environments.
# LEDGERLY_DEV_SHOW_OTP=true

# Email (Resend) — optional in dev; OTP codes are logged to the console if absent.
# Get a key at https://resend.com/api-keys
RESEND_API_KEY=

# Sentry (optional) — error tracking. Get a DSN at https://sentry.io
# SENTRY_DSN=https://your-key@sentry.io/project-id

# Logging level (trace, debug, info, warn, error) — default: info
# LOG_LEVEL=info

# The "from" address for OTP emails. On the Resend free plan, you can only send
# from the shared test address (onboarding@resend.dev) to your own signup email.
# Once you verify a domain at https://resend.com/domains, set this to your
# verified address, e.g.  Ledgerly <noreply@yourdomain.com>
# RESEND_FROM_EMAIL=Ledgerly <noreply@yourdomain.com>
